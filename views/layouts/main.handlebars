<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>purpleturtle</title>
    <!-- Bootstrap CSS link -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>

<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <div class="row justify-content-between align-items-center w-100">
                <!-- Navigation tabs on the left -->
                <div class="col-auto">
                    <ul class="navbar-nav d-flex">
                        {{#if LoggedIn}}
                            <li class="nav-item">
                                <a href="/" class="nav-link text-white">Home</a>
                            </li>
                            <li class="nav-item">
                                <a href="/tickets" class="nav-link text-white">Tickets</a>
                            </li>
                        {{/if}}
                    </ul>
                </div>

                <!-- Centered image within the navigation bar -->
                <div class="col-auto">
                    <a class="navbar-brand" href="#">
                        <img src="/images/purpleturtle.png" alt="Purple Turtle Logo" class="img-fluid" style="max-width: 75%;">
                    </a>
                </div>

                <!-- Log In, Log Out, and Create Account Buttons on the Far Right -->
                <div class="col-auto">
                    {{!-- if logged the logout button will be in the top right, if not the log in and create account will be there  --}}
                    {{#if loggedIn}}
                        <button class="btn btn-outline-light" id="logout">logout</button>
                    {{else}}
                        <a href="/login" class="btn btn-outline-light">Log In</a>
                        <a href="/createaccount" class="btn btn-outline-light">Create Account</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </nav>

    <main class="container container-fluid mt-5">
        {{{ body }}}
    </main>

    <footer class="navbar navbar-dark bg-dark">
        <div class="container">
            <div class="row justify-content-center align-items-center w-100">
                <!-- Centered and shrunk image within the footer -->
                <div class="col-auto">
                    <img src="/images/purpleturtle.png" alt="Purple Turtle Logo" class="img-fluid" style="width: 25%;">
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS and Popper.js scripts (required for some Bootstrap components) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>

